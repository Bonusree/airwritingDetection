{
  "name": "airwriting-collector",
  "version": "1.0.0",
  "private": true,
  "description": "Browser based air-writing collector with GitHub uploads.",
  "scripts": {
    "build": "echo \"No build step required\"",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
